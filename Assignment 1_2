// Use of Doubly Linked List for implementation of Contact Management System
#include <iostream>
using namespace std;

class myNode
{
public:
    string number;
    string name;
    string emailID;
    myNode *next;
    myNode *prev;
};

class myList
{
    myNode *head;

public:
    myList();
    ~myList();
    myNode *getNode();
    void create();
    void insertAtHead();
    void insertInBetween();
    void insertAtTail();
    void display();
    void search();
    void reverse();
    void deleteNode();
};
// Constructor
myList::myList()
{
    head = NULL;
}
// Destructor
myList::~myList()
{
    myNode *p;
    while (head != NULL)
    {
        p = head->next;
        delete head;
        head = p;
    }
}
// Returns Node
myNode *myList::getNode()
{
    myNode *t = new myNode;

    // Phone number validation
    while (true)
    {
        cout << "Enter phone number (10 digits): ";
        cin >> t->number;
        bool correct = t->number.size() == 10;
        for (char c : t->number)
            if (!isdigit(c))
                correct = false;
        if (correct)
            break;
        cout << "Invalid phone number.\n";
    }
    cin.ignore();

    // Name validation
    while (true)
    {
        cout << "Enter name: ";
        getline(cin, t->name);
        bool valid = !t->name.empty();
        for (char c : t->name)
            if (!isalpha(c) && c != ' ')
                valid = false;
        if (valid)
            break;
        cout << "Invalid name. Only alphabets and spaces allowed.\n";
    }

    cout << "Enter email ID: ";
    getline(cin, t->emailID);

    t->next = NULL;
    t->prev = NULL;
    return t;
}
// Create list
void myList::create()
{
    int n;
    cout << "Enter number of nodes to be created: ";
    cin >> n;
    cin.ignore();

    for (int i = 0; i < n; i++)
    {
        myNode *t = getNode();
        if (head == NULL)
            head = t;
        else
        {
            myNode *p = head;
            while (p->next != NULL)
                p = p->next;
            p->next = t;
            t->prev = p;
        }
    }
}
// Insert at head function
void myList::insertAtHead()
{
    myNode *t = getNode();
    t->next = head;
    if (head != NULL)
        head->prev = t;
    head = t;
}
// Insert in between function
void myList::insertInBetween()
{
    string afterName;
    cout << "Insert after which name: ";
    cin.ignore();
    getline(cin, afterName);

    myNode *p = head;
    while (p != NULL && p->name != afterName)
        p = p->next;

    if (p == NULL)
    {
        cout << "Name not found.\n";
        return;
    }

    myNode *t = getNode();
    t->next = p->next;
    t->prev = p;

    if (p->next != NULL)
        p->next->prev = t;

    p->next = t;

    cout << "Contact inserted after " << afterName << ".\n";
}

// Insert At tail function
void myList::insertAtTail()
{
    myNode *t = getNode();

    if (head == NULL)
        head = t;
    else
    {
        myNode *p = head;
        while (p->next != NULL)
            p = p->next;
        p->next = t;
        t->prev = p;
    }
}
// Display function
void myList::display()
{
    if (head == NULL)
    {
        cout << "List is empty.\n";
        return;
    }

    cout << "\n--- Contact List ---\n";
    myNode *p = head;
    while (p != NULL)
    {
        cout << "Number : " << p->number << "\n";
        cout << "Name   : " << p->name << "\n";
        cout << "Email  : " << p->emailID << "\n\n";
        p = p->next;
    }
}
// Search by name
void myList::search()
{
    string targetName;
    cout << "Enter name to be searched: ";
    cin.ignore();
    getline(cin, targetName);

    myNode *p = head;
    while (p != NULL)
    {
        if (p->name == targetName)
        {
            cout << "Contact found:\n";
            cout << "Number: " << p->number << "\nEmail: " << p->emailID << "\n";
            return;
        }
        p = p->next;
    }
    cout << "Data not found\n";
}

// Reverse function
void myList::reverse()
{
    if (head == NULL || head->next == NULL)
        return;

    myNode *curr = head, *temp = NULL;
    while (curr != NULL)
    {
        temp = curr->prev;
        curr->prev = curr->next;
        curr->next = temp;
        curr = curr->prev;
    }
    if (temp != NULL)
        head = temp->prev;

    cout << "List reversed.\n";
    display();
}
// Delete Node function
void myList::deleteNode()
{
    string d;
    cout << "Enter contact number to delete: ";
    cin >> d;

    myNode *p = head;

    while (p != NULL && p->number != d)
        p = p->next;

    if (p == NULL)
    {
        cout << "Data not found\n";
        return;
    }

    if (p->prev != NULL)
        p->prev->next = p->next;
    else
        head = p->next; // Delete head

    if (p->next != NULL)
        p->next->prev = p->prev;

    delete p;
    cout << "Successfully deleted\n";
}

int main()
{
    myList l;
    int choice;

    l.create();

    do
    {
        cout << "\n--- MENU ---\n";
        cout << "1. Insert at head\n";
        cout << "2. Insert in between\n";
        cout << "3. Insert at tail\n";
        cout << "4. Display\n";
        cout << "5. Search\n";
        cout << "6. Reverse\n";
        cout << "7. Delete node\n";
        cout << "8. Exit\n";
        cout << "Enter your choice: ";
        cin >> choice;

        switch (choice)
        {
        case 1:
            l.insertAtHead();
            break;
        case 2:
            l.insertInBetween();
            break;
        case 3:
            l.insertAtTail();
            break;
        case 4:
            l.display();
            break;
        case 5:
            l.search();
            break;
        case 6:
            l.reverse();
            break;
        case 7:
            l.deleteNode();
            break;
        case 8:
            cout << "Exited the program.\n";
            break;
        default:
            cout << "Enter a valid choice.\n";
        }

    } while (choice != 8);

    return 0;
}

/*
Output :
Enter number of nodes to be created: 3
Enter phone number (10 digits): 9011207819
Enter name: om
Enter email ID: om@gmail.com
Enter phone number (10 digits): 9822737056
Enter name: manisha
Enter email ID: manisha@gmail.com 
Enter phone number (10 digits): 9767442979
Enter name: pravin
Enter email ID: pravin@gmail.com

--- MENU ---
1. Insert at head
2. Insert in between
3. Insert at tail
4. Display
5. Search
6. Reverse
7. Delete node
8. Exit
Enter your choice: 4

--- Contact List ---
Number : 9011207819
Name   : om
Email  : om@gmail.com

Number : 9822737056
Name   : manisha
Email  : manisha@gmail.com

Number : 9767442979
Name   : pravin
Email  : pravin@gmail.com


--- MENU ---
1. Insert at head
2. Insert in between
3. Insert at tail
4. Display
5. Search
6. Reverse
7. Delete node
8. Exit
Enter your choice: 5
Enter name to be searched: om
Contact found:
Number: 9011207819
Email: om@gmail.com

--- MENU ---
1. Insert at head
2. Insert in between
3. Insert at tail
4. Display
5. Search
6. Reverse
7. Delete node
8. Exit
Enter your choice: 6
List reversed.

--- Contact List ---
Number : 9767442979
Name   : pravin
Email  : pravin@gmail.com

Number : 9822737056
Name   : manisha
Email  : manisha@gmail.com

Number : 9011207819
Name   : om
Email  : om@gmail.com


--- MENU ---
1. Insert at head
2. Insert in between
3. Insert at tail
4. Display
5. Search
6. Reverse
7. Delete node
8. Exit
Enter your choice: 
7
Enter contact number to delete: 9011207819
Successfully deleted

--- MENU ---
1. Insert at head
2. Insert in between
3. Insert at tail
4. Display
5. Search
6. Reverse
7. Delete node
8. Exit
Enter your choice: 1
Enter phone number (10 digits): 9011207819
Enter name: om
Enter email ID: om@gmail.com

--- MENU ---
1. Insert at head
2. Insert in between
3. Insert at tail
4. Display
5. Search
6. Reverse
7. Delete node
8. Exit
Enter your choice: 4

--- Contact List ---
Number : 9011207819
Name   : om
Email  : om@gmail.com

Number : 9767442979
Name   : pravin
Email  : pravin@gmail.com

Number : 9822737056
Name   : manisha
Email  : manisha@gmail.com


--- MENU ---
1. Insert at head
2. Insert in between
3. Insert at tail
4. Display
5. Search
6. Reverse
7. Delete node
8. Exit
Enter your choice: 2
Insert after which name: pravin
Enter phone number (10 digits): 2367498730
Enter name: vibhav
Enter email ID: vibhav@gmail.com
Contact inserted after pravin.

--- MENU ---
1. Insert at head
2. Insert in between
3. Insert at tail
4. Display
5. Search
6. Reverse
7. Delete node
8. Exit
Enter your choice: 3
Enter phone number (10 digits): 2398056793
Enter name: atharva
Enter email ID: atharva@gmail.com

--- MENU ---
1. Insert at head
2. Insert in between
3. Insert at tail
4. Display
5. Search
6. Reverse
7. Delete node
8. Exit
Enter your choice: 4

--- Contact List ---
Number : 9011207819
Name   : om
Email  : om@gmail.com

Number : 9767442979
Name   : pravin
Email  : pravin@gmail.com

Number : 2367498730
Name   : vibhav
Email  : vibhav@gmail.com

Number : 9822737056
Name   : manisha
Email  : manisha@gmail.com

Number : 2398056793
Name   : atharva
Email  : atharva@gmail.com
*/
